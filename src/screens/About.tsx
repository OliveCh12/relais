import { ScrollView, Text } from 'react-native';
export default function About() {
  return (
    <ScrollView>
      <Text>
        Relais connects a camera phone to a monitor. Photos and videos stay on the camera phone.
      </Text>
    </ScrollView>
  );
}
